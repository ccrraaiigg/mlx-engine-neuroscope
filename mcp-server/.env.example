# MLX Engine Configuration
MLX_ENGINE_API_URL=http://localhost:8080
MLX_ENGINE_API_KEY=optional-api-key

# MCP Server Configuration
MCP_PORT=3000
MCP_HOST=localhost

# Logging Configuration
LOG_LEVEL=INFO

# Note: Anthropic API key is loaded from the 'keys' file
# MLX Engine must be running for real analysis
# Failures will show exactly what needs to be implemented

# Example usage:
# 1. Ensure MLX Engine is running on localhost:8080
# 2. Run: npm run chatbot
# 3. Try analysis commands to see what fails